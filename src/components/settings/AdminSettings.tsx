
import React from 'react';

// Import the existing Settings component for admin use
import Settings from '@/pages/Settings';

const AdminSettings = () => {
  return <Settings />;
};

export default AdminSettings;
